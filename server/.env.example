# Copy this file to .env and set your values.
# The server loads .env from the server/ directory.

# Server port (default: 3000)
PORT=3000

# OpenAI (required if your endpoint uses the OpenAI client)
# POE_API_KEY=sk-...
