# Frontend (Vite): only variables prefixed with VITE_ are exposed to the client.
# Use import.meta.env.VITE_* in your app.

# Example:
# VITE_API_URL=http://localhost:3000

# Backend (Node): if you run a server, use dotenv and load this file.
# Add server-only variables below (no VITE_ prefix).
